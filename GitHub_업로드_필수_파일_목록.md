# GitHub 업로드 필수 파일 목록

> 시스템 실행에 필요한 파일들만 선별하여 업로드하세요

---

## ✅ 반드시 업로드해야 할 파일

### 1. 소스 코드 디렉토리

```
app/                      # Next.js 앱 디렉토리 (전체)
├── (auth)/              # 로그인/회원가입 페이지
├── (dashboard)/         # 대시보드 페이지들
├── api/                 # API Routes (전체)
├── globals.css          # 글로벌 스타일
├── layout.tsx           # 루트 레이아웃
└── page.tsx             # 홈페이지

components/               # React 컴포넌트들
├── AdCollectionDashboard.tsx
├── CopyFeedbackButtons.tsx
└── Navbar.tsx

lib/                      # 유틸리티 라이브러리
├── auth/                # 인증 관련
├── config/              # 설정
├── security/            # 보안
└── supabase/            # Supabase 클라이언트

src/                      # 핵심 비즈니스 로직
├── application/         # 애플리케이션 서비스
│   └── services/        # 비즈니스 로직 서비스들
├── domain/              # 도메인 모델
│   ├── ad-examples/
│   ├── ad-platforms/
│   └── copywriting/
└── infrastructure/      # 인프라 계층
    ├── ad-platforms/
    ├── ai/              # AI 프로바이더
    ├── database/        # DB 스키마
    └── scraping/

public/                   # 정적 파일 (있는 경우)

scripts/                  # 유틸리티 스크립트
└── setup-encoding.js
```

### 2. 설정 파일

```
package.json              # ✅ 의존성 관리 (requirements.txt 역할)
tsconfig.json            # TypeScript 설정
next.config.mjs          # Next.js 설정
tailwind.config.ts       # Tailwind CSS 설정
postcss.config.mjs       # PostCSS 설정
drizzle.config.ts        # Drizzle ORM 설정
```

### 3. 환경 변수 템플릿

```
env.example.txt          # ✅ 환경 변수 템플릿 (실제 값 제외)
```

### 4. Git 관련 파일

```
.gitignore               # Git 무시 파일 목록
```

### 5. 문서 파일

```
README.md                        # 프로젝트 설명
AWS_EC2_배포_가이드.md          # 배포 가이드
배포_빠른_시작_가이드.md        # 빠른 배포 가이드
배포_체크리스트.md              # 체크리스트
배포_가이드_모음.md             # 가이드 모음
GitHub_업로드_가이드.md         # GitHub 업로드 가이드
시스템_아키텍처.md              # 시스템 아키텍처 (선택)
TODO.md                          # 할 일 목록 (선택)
```

---

## ❌ 절대 업로드하면 안 되는 파일

### 1. 환경 변수 (API 키 포함!)

```
❌ env.local.txt         # 실제 API 키 포함
❌ .env.local            # 환경 변수
❌ .env                  # 환경 변수
```

### 2. 의존성 및 빌드 파일

```
❌ node_modules/         # npm 패키지 (용량 큼, npm install로 설치)
❌ .next/                # Next.js 빌드 결과물
❌ package-lock.json     # 자동 생성됨
```

### 3. 로그 및 임시 파일

```
❌ *.log                 # 로그 파일
❌ server.log
❌ *.tmp
❌ *.temp
```

### 4. 테스트 파일

```
❌ test*.js
❌ test*.ps1
❌ test*.mjs
❌ test*.bat
❌ watch-*.ps1
❌ *.backup
```

### 5. 로컬 스크립트

```
❌ start-server.bat
❌ start-test-server.bat
❌ auto-setup-and-start.bat
❌ ONE-CLICK-START.bat
❌ one-click-start.sh
❌ restart-server.ps1
❌ start-new-environment.ps1
❌ check-environment.bat
```

### 6. 데이터베이스 파일

```
❌ drizzle/              # 마이그레이션 히스토리 (Supabase에서 관리)
❌ *.sql                 # SQL 파일들
❌ fix-users-sequence.sql
```

### 7. 로컬 전용 문서 (이미 많음)

```
❌ 개선_완료_요약.md
❌ 문제해결_가이드.md
❌ 설치_가이드.md
❌ 슈퍼베이스_*.md
❌ 시스템_보고서*.md
❌ 최종_개선_완료_보고서.md
❌ JWT_토큰_오류_*.md
❌ Pltt_AD_Copy_*.md
❌ system docs/           # 시스템 문서 폴더
```

---

## 📦 의존성 관리 (requirements.txt 대신)

Node.js 프로젝트는 `package.json`이 Python의 `requirements.txt` 역할을 합니다.

### package.json (이미 있음)

```json
{
  "name": "pltt-ad-copy",
  "version": "0.1.0",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.1",
    "@supabase/supabase-js": "^2.39.3",
    "bcryptjs": "^2.4.3",
    "cheerio": "^1.1.2",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.29.3",
    "jsonwebtoken": "^9.0.2",
    "next": "^14.2.5",
    "postgres": "^3.4.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^3.22.4"
  }
}
```

**설치 방법:**
```bash
npm install
```

---

## 📊 파일 크기 기준

### GitHub 업로드 제한
- **단일 파일**: 100MB 이하
- **저장소 전체**: 1GB 권장 (5GB까지 가능)

### 현재 프로젝트 (필수 파일만)
- **소스 코드**: 약 5-10MB
- **설정 파일**: 약 1MB
- **문서**: 약 1-2MB
- **총 예상 크기**: **10-15MB** (node_modules 제외)

---

## ✅ 업로드 전 최종 확인

### 1. .gitignore 체크

```bash
# .gitignore가 제대로 설정되었는지 확인
cat .gitignore
```

### 2. Git 상태 확인

```bash
git status
```

**확인 사항:**
- ❌ `env.local.txt`가 스테이징되지 않았는지
- ❌ `node_modules/`가 스테이징되지 않았는지
- ❌ `.next/`가 스테이징되지 않았는지
- ✅ `app/`, `src/`, `components/`, `lib/`가 포함되었는지

### 3. 필수 파일 존재 확인

```bash
# 필수 파일들이 있는지 확인
ls app/
ls src/
ls components/
ls lib/
ls package.json
ls env.example.txt
```

---

## 🚀 GitHub 업로드 순서

### 1. Git 초기화

```bash
git init
```

### 2. 파일 스테이징

```bash
git add .
```

### 3. 확인

```bash
git status
```

### 4. 커밋

```bash
git commit -m "Initial commit: 광고 소재 문구 생성 시스템"
```

### 5. 원격 저장소 연결

```bash
git remote add origin https://github.com/your-username/ad-copy-generator.git
git branch -M main
```

### 6. 푸시

```bash
git push -u origin main
```

---

## 📁 최종 업로드 파일 트리

```
광고 소제 문구 생성/
├── app/                          ✅ 필수
├── components/                   ✅ 필수
├── lib/                          ✅ 필수
├── src/                          ✅ 필수
├── public/                       ✅ 필수 (있는 경우)
├── scripts/                      ✅ 필수
├── .gitignore                    ✅ 필수
├── package.json                  ✅ 필수 (의존성 관리)
├── tsconfig.json                 ✅ 필수
├── next.config.mjs               ✅ 필수
├── tailwind.config.ts            ✅ 필수
├── postcss.config.mjs            ✅ 필수
├── drizzle.config.ts             ✅ 필수
├── env.example.txt               ✅ 필수 (템플릿)
├── README.md                     ✅ 권장
├── AWS_EC2_배포_가이드.md        ✅ 권장
├── 배포_빠른_시작_가이드.md      ✅ 권장
├── 배포_체크리스트.md            ✅ 권장
├── 배포_가이드_모음.md           ✅ 권장
├── GitHub_업로드_가이드.md       ✅ 권장
├── 시스템_아키텍처.md            ⚪ 선택
└── TODO.md                       ⚪ 선택
```

**총 파일 개수**: 약 100-150개 (디렉토리 포함)  
**예상 크기**: 10-15MB

---

## 🔍 업로드 후 확인

GitHub 저장소에서 확인:

1. ✅ `app/` 디렉토리가 있는지
2. ✅ `src/` 디렉토리가 있는지
3. ✅ `package.json`이 있는지
4. ✅ `env.example.txt`가 있는지 (실제 API 키 없는 템플릿)
5. ❌ `node_modules/`가 **없는지**
6. ❌ `env.local.txt`가 **없는지**
7. ❌ `.next/`가 **없는지**

---

**마지막 업데이트**: 2025-11-18  
**문서 버전**: 1.0.0

