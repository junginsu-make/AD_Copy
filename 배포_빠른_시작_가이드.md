# 배포 빠른 시작 가이드

> 광고 소재 문구 생성 시스템을 **15분 안에** AWS EC2에 배포하기

---

## 📋 사전 준비 (5분)

### 1. GitHub 준비
```powershell
# 현재 디렉토리 확인
cd "C:\Users\a2061\Desktop\Coding\바탕 화면\광고 소제 문구 생성"

# Git 초기화 및 업로드
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/ad-copy-generator.git
git branch -M main
git push -u origin main
```

### 2. AWS EC2 인스턴스 생성
- **AMI**: Ubuntu Server 22.04 LTS
- **Instance Type**: t3.medium (권장) 또는 t2.micro (테스트)
- **Storage**: 30GB
- **Security Group**: HTTP(80), HTTPS(443), SSH(22), Custom(3000)
- **Key Pair**: 다운로드 후 안전하게 보관

---

## 🚀 EC2 배포 (10분)

### Step 1: SSH 접속
```powershell
ssh -i "C:\path\to\your-key.pem" ubuntu@your-ec2-ip
```

### Step 2: 원클릭 설치 스크립트
EC2에 접속한 후 다음 명령어 실행:

```bash
# 시스템 업데이트 및 Node.js 설치
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash - && \
sudo apt update && \
sudo apt install -y nodejs git nginx && \
sudo npm install -g pm2

# 저장소 클론
cd ~ && \
git clone https://github.com/your-username/ad-copy-generator.git && \
cd ad-copy-generator
```

### Step 3: 환경 변수 설정
```bash
# .env.local 파일 생성 (nano 에디터 열림)
nano .env.local
```

**최소 필수 환경 변수 (복사 후 실제 값으로 수정):**
```bash
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_key
DATABASE_URL=your_database_url
JWT_SECRET=random-secret-key-change-this
OPENAI_API_KEY=your_openai_key
ANTHROPIC_API_KEY=your_anthropic_key
GEMINI_API_KEY_1=your_gemini_key
```

저장: `Ctrl + X` → `Y` → `Enter`

### Step 4: 빌드 및 실행
```bash
# 의존성 설치 및 빌드
npm install && npm run build

# PM2로 실행
pm2 start npm --name "ad-copy-generator" -- start
pm2 startup
pm2 save
```

### Step 5: Nginx 설정
```bash
# Nginx 설정 파일 생성
sudo tee /etc/nginx/sites-available/ad-copy-generator > /dev/null <<EOF
server {
    listen 80;
    server_name _;
    client_max_body_size 10M;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade \$http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host \$host;
        proxy_cache_bypass \$http_upgrade;
        proxy_read_timeout 300s;
    }
}
EOF

# Nginx 활성화
sudo ln -s /etc/nginx/sites-available/ad-copy-generator /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t && sudo systemctl restart nginx
```

---

## ✅ 배포 완료 확인

브라우저에서 접속:
```
http://your-ec2-public-ip
```

---

## 🔧 자주 사용하는 명령어

```bash
# 로그 확인
pm2 logs ad-copy-generator

# 재시작
pm2 restart ad-copy-generator

# 상태 확인
pm2 status

# Nginx 재시작
sudo systemctl restart nginx

# 코드 업데이트 (GitHub에서 pull)
cd ~/ad-copy-generator
git pull
npm install
npm run build
pm2 restart ad-copy-generator
```

---

## 🆘 문제 해결

### 포트 3000 사용 중
```bash
sudo lsof -i :3000
pm2 restart ad-copy-generator
```

### 빌드 오류
```bash
rm -rf node_modules .next
npm install
npm run build
```

### Nginx 오류
```bash
sudo tail -f /var/log/nginx/error.log
```

---

## 📚 상세 가이드

더 자세한 내용은 `AWS_EC2_배포_가이드.md` 파일을 참고하세요.

---

**배포 완료 시간**: 약 15분  
**권장 인스턴스**: t3.medium 이상  
**최소 메모리**: 2GB RAM

