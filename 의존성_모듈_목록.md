# 의존성 모듈 목록

> Node.js 프로젝트의 의존성 관리 (Python의 requirements.txt에 해당)

---

## 📦 package.json (이미 존재)

Node.js 프로젝트에서는 `package.json` 파일이 Python의 `requirements.txt` 역할을 합니다.

**별도로 requirements.txt를 만들 필요가 없습니다!**

---

## 🔧 현재 설치된 의존성

### 프로덕션 의존성 (dependencies)

```json
{
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.1",       // Claude AI
    "@supabase/supabase-js": "^2.39.3",   // Supabase 클라이언트
    "@types/cheerio": "^0.22.35",         // Cheerio 타입
    "bcryptjs": "^2.4.3",                 // 비밀번호 해싱
    "cheerio": "^1.1.2",                  // HTML 파싱
    "dotenv": "^16.4.5",                  // 환경 변수 관리
    "drizzle-kit": "^0.20.12",            // Drizzle ORM CLI
    "drizzle-orm": "^0.29.3",             // Drizzle ORM
    "jsonwebtoken": "^9.0.2",             // JWT 토큰
    "next": "^14.2.5",                    // Next.js 프레임워크
    "postgres": "^3.4.3",                 // PostgreSQL 클라이언트
    "react": "^18.3.1",                   // React
    "react-dom": "^18.3.1",               // React DOM
    "zod": "^3.22.4"                      // 스키마 검증
  }
}
```

### 개발 의존성 (devDependencies)

```json
{
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",          // bcryptjs 타입
    "@types/jsonwebtoken": "^9.0.5",      // jsonwebtoken 타입
    "@types/node": "^20.14.12",           // Node.js 타입
    "@types/react": "^18.3.3",            // React 타입
    "@types/react-dom": "^18.3.0",        // React DOM 타입
    "autoprefixer": "^10.4.19",           // CSS autoprefixer
    "eslint": "^8.57.0",                  // ESLint
    "eslint-config-next": "^14.2.5",      // Next.js ESLint 설정
    "postcss": "^8.4.39",                 // PostCSS
    "prettier": "^3.3.3",                 // 코드 포매터
    "prettier-plugin-tailwindcss": "^0.6.4", // Tailwind Prettier
    "tailwindcss": "^3.4.6",              // Tailwind CSS
    "typescript": "^5.5.4"                // TypeScript
  }
}
```

---

## 🚀 모듈 설치 방법

### 새 환경에서 설치

```bash
# package.json에 정의된 모든 모듈 설치
npm install

# 또는 (yarn 사용 시)
yarn install

# 또는 (pnpm 사용 시)
pnpm install
```

### 특정 모듈 추가 설치

```bash
# 프로덕션 의존성 추가
npm install 패키지명

# 개발 의존성 추가
npm install --save-dev 패키지명
```

---

## 📋 AI API SDK

### 필수 AI 모듈

| 모듈 | 버전 | 용도 |
|------|------|------|
| `@anthropic-ai/sdk` | ^0.20.1 | Claude AI (대화형 카피 생성) |
| `@google/generative-ai` | ❌ 미설치 | Gemini AI (설치 필요) |
| `openai` | ❌ 미설치 | OpenAI GPT (설치 필요) |

### 🔴 누락된 모듈 설치 필요

현재 `package.json`에 다음 모듈이 누락되어 있습니다:

```bash
# OpenAI SDK 설치
npm install openai

# Google Gemini SDK 설치
npm install @google/generative-ai

# Perplexity는 HTTP 요청으로 사용 (별도 SDK 불필요)
```

---

## 🌐 웹 스크래핑 모듈

### 현재 설치됨

| 모듈 | 버전 | 용도 |
|------|------|------|
| `cheerio` | ^1.1.2 | HTML 파싱 |

### 권장 추가 모듈

```bash
# Axios (HTTP 클라이언트)
npm install axios

# Firecrawl SDK (웹 스크래핑)
npm install @mendable/firecrawl-js
```

---

## 🗄️ 데이터베이스 관련

### 현재 설치됨

| 모듈 | 버전 | 용도 |
|------|------|------|
| `@supabase/supabase-js` | ^2.39.3 | Supabase 클라이언트 |
| `drizzle-orm` | ^0.29.3 | ORM |
| `drizzle-kit` | ^0.20.12 | Drizzle CLI |
| `postgres` | ^3.4.3 | PostgreSQL 드라이버 |

---

## 🔒 보안 관련

### 현재 설치됨

| 모듈 | 버전 | 용도 |
|------|------|------|
| `bcryptjs` | ^2.4.3 | 비밀번호 해싱 |
| `jsonwebtoken` | ^9.0.2 | JWT 인증 |
| `zod` | ^3.22.4 | 입력 검증 |
| `dotenv` | ^16.4.5 | 환경 변수 관리 |

---

## ✅ 완전한 package.json 업데이트

누락된 모듈을 포함한 완전한 버전:

```json
{
  "name": "pltt-ad-copy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push --config=drizzle.config.ts",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.1",
    "@google/generative-ai": "^0.1.3",
    "@mendable/firecrawl-js": "^0.0.19",
    "@supabase/supabase-js": "^2.39.3",
    "@types/cheerio": "^0.22.35",
    "axios": "^1.6.2",
    "bcryptjs": "^2.4.3",
    "cheerio": "^1.1.2",
    "dotenv": "^16.4.5",
    "drizzle-kit": "^0.20.12",
    "drizzle-orm": "^0.29.3",
    "jsonwebtoken": "^9.0.2",
    "next": "^14.2.5",
    "openai": "^4.20.1",
    "postgres": "^3.4.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/node": "^20.14.12",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.19",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.5",
    "postcss": "^8.4.39",
    "prettier": "^3.3.3",
    "prettier-plugin-tailwindcss": "^0.6.4",
    "tailwindcss": "^3.4.6",
    "typescript": "^5.5.4"
  }
}
```

---

## 🔄 모듈 업데이트

### 전체 모듈 업데이트

```bash
# 최신 버전으로 업데이트
npm update

# 메이저 버전 업데이트 확인
npm outdated
```

### 보안 취약점 확인

```bash
# 보안 취약점 검사
npm audit

# 자동 수정
npm audit fix
```

---

## 📊 모듈 크기 확인

```bash
# node_modules 크기 확인
du -sh node_modules/

# 또는 Windows PowerShell
Get-ChildItem node_modules -Recurse | Measure-Object -Property Length -Sum
```

**예상 크기**: 약 300-500MB

---

## 🚨 중요 사항

### 1. node_modules는 Git에 올리지 마세요!

`.gitignore`에 다음이 포함되어 있어야 합니다:

```gitignore
node_modules/
package-lock.json
```

### 2. EC2에서 설치 방법

```bash
# 저장소 클론 후
cd ad-copy-generator

# 모든 의존성 설치
npm install

# 빌드
npm run build

# 실행
npm start
```

### 3. 누락된 모듈 추가

현재 프로젝트에서 사용 중이지만 `package.json`에 없는 모듈:

```bash
npm install openai @google/generative-ai axios @mendable/firecrawl-js
```

---

## 📝 package.json vs requirements.txt 비교

| 항목 | Python (requirements.txt) | Node.js (package.json) |
|------|---------------------------|------------------------|
| 파일명 | requirements.txt | package.json |
| 설치 명령 | `pip install -r requirements.txt` | `npm install` |
| 패키지 추가 | 수동 편집 | `npm install 패키지명` |
| 버전 관리 | 수동 | 자동 |
| 잠금 파일 | requirements-lock.txt | package-lock.json |

---

**마지막 업데이트**: 2025-11-18  
**문서 버전**: 1.0.0

